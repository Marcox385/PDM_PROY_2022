import 'package:flutter/material.dart';

class HacerCita extends StatelessWidget {
  const HacerCita({super.key});

  @override
  Widget build(BuildContext context) {
    return  Text('Hacer cita works!');
  }
}
